# PostgreSQL Database Configuration
# Copy this file to .env.postgres and update with your actual credentials

# Database Connection
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=placement_management

# Connection Pool Settings (optional)
POSTGRES_MAX_CONNECTIONS=20
POSTGRES_IDLE_TIMEOUT=30000
POSTGRES_CONNECTION_TIMEOUT=2000

# SSL Configuration (for production)
POSTGRES_SSL=false
# POSTGRES_SSL_CA=/path/to/ca-certificate.crt
# POSTGRES_SSL_CERT=/path/to/client-certificate.crt
# POSTGRES_SSL_KEY=/path/to/client-key.key

# Migration Settings
POSTGRES_MIGRATIONS_TABLE=schema_migrations
POSTGRES_MIGRATIONS_DIR=./migrations

# Database URL (alternative to individual settings)
# Format: postgresql://username:password@host:port/database
# DATABASE_URL=postgresql://postgres:password@localhost:5432/placement_management

# ============================================================================
# IMPORTANT NOTES:
# ============================================================================
# 1. Never commit this file with actual credentials to version control
# 2. Add .env.postgres to .gitignore
# 3. Use strong passwords in production
# 4. Enable SSL for production databases
# 5. Restrict database user permissions appropriately
# 6. Regularly backup your database
# ============================================================================
