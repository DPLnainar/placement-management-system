# AWS S3 Configuration Example
# Copy this to your .env file and replace with your actual values

# ============================================
# AWS S3 Configuration
# ============================================

# AWS Access Key ID (from IAM user)
AWS_ACCESS_KEY_ID=your-access-key-id-here

# AWS Secret Access Key (from IAM user)
AWS_SECRET_ACCESS_KEY=your-secret-access-key-here

# AWS Region (e.g., us-east-1, ap-south-1, eu-west-1)
AWS_REGION=us-east-1

# S3 Bucket Name (must be globally unique)
AWS_S3_BUCKET_NAME=your-bucket-name-here

# Optional: CloudFront Distribution URL (for faster content delivery)
# Leave empty if not using CloudFront
AWS_CLOUDFRONT_URL=

# ============================================
# Storage Provider Selection
# ============================================

# Set to 'true' to use S3, 'false' to use Cloudinary
USE_S3=true

# ============================================
# Other Environment Variables
# ============================================
# (Keep your other existing environment variables below)

PORT=5000
NODE_ENV=development
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRE=7d

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/placement
# OR for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Cloudinary Configuration (if still using for some files)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Frontend URL
FRONTEND_URL=http://localhost:3000

# ============================================
# AWS S3 Setup Instructions
# ============================================
# 
# 1. Create an AWS account at https://aws.amazon.com/
# 
# 2. Create an S3 bucket:
#    - Go to S3 in AWS Console
#    - Click "Create bucket"
#    - Choose a unique name
#    - Select your region
#    - Configure permissions (public or private)
#    - Click "Create bucket"
# 
# 3. Create an IAM user:
#    - Go to IAM in AWS Console
#    - Click "Users" → "Create user"
#    - Attach "AmazonS3FullAccess" policy
#    - Create access keys
#    - Save Access Key ID and Secret Access Key
# 
# 4. Update this file with your credentials
# 
# 5. Install dependencies:
#    npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner uuid
#    npm install --save-dev @types/uuid
# 
# 6. Test the connection:
#    npx ts-node test-s3-connection.ts
# 
# For detailed setup instructions, see AWS_S3_SETUP.md
