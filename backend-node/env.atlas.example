# MongoDB Atlas Environment Configuration Example

# This file shows the format for MongoDB Atlas connection string
# Copy this to your .env file and replace with your actual values

# ============================================
# MongoDB Atlas Configuration
# ============================================

# MongoDB Atlas Connection String
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
# 
# Replace:
# - <username>: Your database username (e.g., placement-admin)
# - <password>: Your database password (URL-encode special characters!)
# - <cluster-url>: Your cluster URL from Atlas (e.g., cluster0.xxxxx.mongodb.net)
# - <database-name>: Your database name (e.g., placement)
#
# Example:
# MONGODB_URI=mongodb+srv://placement-admin:MyP%40ssw0rd@cluster0.abc123.mongodb.net/placement?retryWrites=true&w=majority

MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# ============================================
# URL Encoding Reference for Special Characters
# ============================================
# If your password contains special characters, encode them:
# @ → %40
# : → %3A
# / → %2F
# ? → %3F
# # → %23
# [ → %5B
# ] → %5D
# % → %25

# ============================================
# Other Environment Variables
# ============================================
# (Keep your other existing environment variables below)

PORT=5000
NODE_ENV=development
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRE=7d

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Cloudinary Configuration (if using)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Frontend URL
FRONTEND_URL=http://localhost:3000
